import 'package:flutter/material.dart';

class AppColors {
  // ─── Brand ────────────────────────────────────────────
  static const Color primary = Color(0xFF0BBE5E);
  static const Color primaryLight = Color(0xFFAEDC81);
  static const Color lightGreen = Color(0xFFEDFAF1);

  // ─── Neutrals ─────────────────────────────────────────
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color gray = Color(0xFF8391A1);
  static const Color lightGrey = Color(0xFFF7F8F9);
  static const Color medGray = Color(0xFFDDDDDD);
  static const Color grey = Color(0xFFC5C9D2);
  static const Color greyBorder = Color(0xFFE8ECF4);
  static const Color f5f6fa = Color(0xFFF5F6FA);

  // ─── Accent ───────────────────────────────────────────
  static const Color red = Color(0xFFE62626);
  static const Color lightRed = Color(0xFFF9A1A1);
  static const Color lightyellow = Color(0xFFFCFEC0);

  // ─── Text ─────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textHint = Color(0xFF94A3B8);

  // ─── Error States ─────────────────────────────────────
  static const Color errorNoInternet = Color(0xFF0284C7);
  static const Color errorTimeout = Color(0xFFD97706);
  static const Color errorUnauthorized = Color(0xFFDC2626);
  static const Color errorForbidden = Color(0xFFEA580C);
  static const Color errorServer = Color(0xFF7C3AED);
}
